# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the checked-in template with no real values (see next section).
!.env.deploy.example
yaga partner program bot
*bot*token*
*.key
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# agent data
/appDataDir/
.gemini/

# telegram session & exports
*.session
*.session-journal
my_old_cmc_members.*
telegram_exports/

# runtime data (server-side conversion-event log, etc.) — never committed
/data/

# secret-adjacent local files flagged in audit-2026/findings/06-security.md
# and 14-infrastructure-devops.md — previously untracked-but-unprotected,
# meaning one `git add -A` away from being committed. Listed explicitly by
# name (rather than only broader patterns) since the existing *bot*token*
# pattern above didn't match this file's actual name.
/yaga client relation bot
/hightable_exports/
/scrape_all_free_members.py
/scrape_deep_subscribers.py
/scrape_remaining_to_5109.py
/searched_with_*.csv

